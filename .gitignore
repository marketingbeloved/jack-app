# Python / env
.venv/
__pycache__/
*.pyc
.DS_Store

# Secrets — NEVER commit
.streamlit/secrets.toml
.bp-credentials/

# Runtime data (lives in Supabase in cloud; local-only on the Mac)
cache/plan_briefs.json
cache/concepts.json
cache/concepts.backup.json

# Jack's few-shot corpus — brand intel, used only for LOCAL Claude generation.
# Not needed in the read-only cloud, keep it off GitHub.
cache/notion_briefs_*.json

# launchd / watchdog logs
*.log

# Приватное — НЕ в публичный репо (остаётся локально на маке)
assets/avatars/vika.*
assets/avatars/dina.*
brand_data/
cache/competitors.json
cache/socials.json
