# Python / env
.venv/
__pycache__/
*.pyc
.DS_Store

# Secrets — NEVER commit
.streamlit/secrets.toml
.bp-credentials/

# Runtime data — ВСЯ папка. Источник правды это Supabase, локальные файлы лишь бэкап,
# и репозиторий ПУБЛИЧНЫЙ: сюда попадают ТЗ, концепты, корпуса и правила Джека.
# Раньше перечислялись отдельные файлы, и каждый новый (jack_rules.json) утекал бы в паблик.
cache/

# launchd / watchdog logs
*.log

# Приватное — НЕ в публичный репо (остаётся локально на маке)
assets/avatars/vika.*
assets/avatars/dina.*
brand_data/
cache/competitors.json
cache/socials.json

.streamlit/config.toml
